import { DomainPagePlaceholder } from "@/shared/ui/feedback/domain-page-placeholder";

// Reserva la vista operativa para la siguiente fase de desarrollo.
export function OperationalPage() {
  return (
    <DomainPagePlaceholder
      title="Operaciones"
      description="Se construira con foco en SLA, estados de orden, tecnicos, sucursales y tiempos de ciclo."
    />
  );
}
