import { DomainPagePlaceholder } from "@/shared/ui/feedback/domain-page-placeholder";

// Reserva la vista CRM para la siguiente fase de desarrollo.
export function CrmPage() {
  return (
    <DomainPagePlaceholder
      title="CRM"
      description="Se construira con foco en clientes, empresas, sucursales y cobertura comercial."
    />
  );
}
