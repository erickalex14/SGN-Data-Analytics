import { DomainPagePlaceholder } from "@/shared/ui/feedback/domain-page-placeholder";

// Reserva la vista de inventario para la siguiente fase de desarrollo.
export function InventoryPage() {
  return (
    <DomainPagePlaceholder
      title="Inventario"
      description="Se construira con foco en stock, consumo por orden, solicitudes y listas de compra."
    />
  );
}
